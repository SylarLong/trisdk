{
  "name": "trisdk",
  "version": "0.0.1",
  "description": "it's SDK of triones",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json",
    "test": "jest --config jest.config.json",
    "prepare": "npm run build && npm run format && npm run lint && npm run test"
  },
  "author": "sylarlong",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/SylarLong/trisdk.git"
  },
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "jest": "^27.0.6",
    "prettier": "^2.3.2",
    "ts-jest": "^27.0.5",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "got": "^11.8.2"
  }
}
